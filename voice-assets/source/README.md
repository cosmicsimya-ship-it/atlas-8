# Voice source assets — IMMUTABLE

Files in this directory are master reference audio.

- Do **not** overwrite, re-encode in place, or delete masters.
- Derivatives belong in `../processed/` and must be regenerable via `scripts/audio/prepare-reference.mjs`.

## Lara

- `lara-master.mp3` — copy of founder master voice (`Master Ses Audio MP3 Lara.mp3`)
- Original download path is left untouched.
